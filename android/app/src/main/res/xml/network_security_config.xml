<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Release traffic: only certs trusted by the OS -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug builds only: also trust CAs installed on the device/emulator,
         so a self-signed cert (e.g. a local dev server) is accepted -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
